class ProductService {
    // async index() { }

    // async show(id) { }

    // async store(data) { }

    // async update(id, data) { }

    // async destroy(id) { }
}

module.exports = new ProductService